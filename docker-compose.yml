services:
  mdbook:
    image: dreamcacao/mdbook:latest
    build:
      context: .
    ports:
      - "3000:3000"
    volumes:
      - ./src:/book/src
      - ./book.toml:/book/book.toml
    command: [ "mdbook", "serve", "--hostname", "0.0.0.0" ]
